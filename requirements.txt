# 런타임 의존성. **직접 import 하는 것만** 적고 정확한 버전으로 고정한다.
# 전이 의존성까지 pip freeze 로 쏟아붓지 않는 이유는 docs/adr/0001 에 있다.
#
#   pip install -r requirements.txt

# 수치 배열 — 패널 인코딩, 지표 계산, 예측 점수
numpy==2.4.6

# 통계 베이스라인 — LLM 이 값을 하는지 비교할 대상
scikit-learn==1.8.0

# 수집한 JSON 스키마 검증, LLM 구조화 출력 파싱 실패 처리
pydantic==2.13.4

# 웹 아카이브 HTML 파싱 (패치 노트)
beautifulsoup4==4.14.2

# .env 로드 — API 키를 코드에 두지 않는다
python-dotenv==1.2.3

# 패치 노트 수치 추출. **실행 시점에는 부르지 않는다** —
# extract.py 가 TYPE_CHECKING 아래에서만 쓰고, pilot-extract 는 지연 import 한다.
# mypy 가 타입을 풀려면 필요하다.
anthropic==1.0.0

# ── 에이전트(B8) ─────────────────────────────────────────────────────
# 근거는 docs/adr/0009-agent-framework-and-local-model.md.
# **기본은 로컬 모델이라 키가 필요 없다.** 다른 제공자는 그 통합 패키지를
# 따로 넣는다(langchain-openai 등) — 여기에는 적지 않는다.

# 에이전트 루프 · 구조화 출력 · 호출 상한 (create_agent · init_chat_model)
langchain==1.4.1

# 메시지 · 프롬프트 틀 · Runnable · @tool — 직접 import 한다
langchain-core==1.6.3

# 해설에 대해 더 묻기 — 대화를 파일에 남긴다 (SqliteSaver)
langgraph-checkpoint-sqlite==3.1.1

# 로컬 모델(Ollama). **직접 import 하지 않는다** — init_chat_model 이
# 「ollama:」 접두사를 보고 불러온다. 이것이 키 없는 길이다
langchain-ollama==1.1.0

# 유료 모델과 견줄 때만 쓴다 — `openai:gpt-4.1-mini`. 키가 없으면 안 불린다.
# 기본 모델은 그대로 로컬이다(ADR 0009).
langchain-openai==1.6.2

# 에이전트 화면 (scripts/app). 과정 과제가 Gradio 를 쓴다
gradio==6.27.0

# 판단 기록에 OpenAI 기준 토큰 수를 적는다 — 키를 쓸 때의 비용 추정용.
# 판단에는 안 쓴다
tiktoken==0.14.0
