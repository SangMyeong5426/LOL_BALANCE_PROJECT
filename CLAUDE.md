# 작업 규칙

이 저장소에서 작업하는 사람과 자동화 에이전트가 따라야 할 규칙이다.

## 이 문서는 두 이름으로 존재한다

[`AGENTS.md`](AGENTS.md)와 [`CLAUDE.md`](CLAUDE.md)는 **같은 문서다.** 한 글자도 달라선 안 된다.

도구마다 읽는 파일이 다르기 때문에 통째로 복제한다. 한쪽만 고친 상태로 두지 않고, 두 파일이 다르면 어느 쪽이 맞는지 임의로 정하지 않고 양쪽 원문을 놓고 판단한다.

**`scripts/check-rule-docs`가 이것을 강제한다.** pre-commit hook 으로 등록돼 있어 두 파일이 다르면 커밋이 막힌다. 검사를 없애려면 이 절도 함께 지운다.

## 무엇을 만드는가

리그 오브 레전드의 과거 패치 이력에서 **개발사의 밸런스 조정 기준을 역추출**하고, 그 기준으로 다음 패치 내용을 예측한 뒤, 실제 패치와 대조해 **적중률을 측정한다.**

**계획서는 아직 없다.** 데이터 확보 가능성을 먼저 확인하는 중이고, 결과는 [`docs/spec/data-sources.md`](docs/spec/data-sources.md)에 있다. **그 문서 8장의 남은 확인을 끝내기 전에 계획서를 쓰지 않는다** — 확보할 수 없는 데이터를 전제로 쓴 계획서는 다시 써야 한다.

## 명세와 코드의 사슬

기준이 되는 문서 하나를 두지 않는다. 이어진 사슬을 맞춰 나간다.

```text
docs/spec/data-sources.md    데이터를 어디서 어떤 형식으로 받는가
       ↕
data/                        받아온 원자료 (커밋하지 않는다)
       ↕
src/                         수집 · 전처리 · 예측 · 평가
       ↕
tests/                       그 처리가 맞는가
```

**붙어 있는 두 칸은 항상 같은 것을 말해야 한다.** 수집 코드를 고치면 `data-sources.md` 의 해당 항목도 고친다. 주소나 형식이 바뀌었는데 문서가 그대로면, 다음에 그 문서를 믿고 작업하는 사람이 헛수고를 한다.

### 어긋나면 임의로 정하지 않는다

출처가 서로 다른 값을 말하거나, 문서와 실제 응답이 다르면 어느 쪽이 사실인지 혼자 정하지 않는다. 판단할 수 없으면 [`docs/followups.md`](docs/followups.md)에 적고 넘어간다. **조용히 지나가지 않는다.**

## 이 프로젝트 고유 규칙

### 원자료를 커밋하지 않는다

아카이브에서 긁어온 HTML·JSON, Oracle's Elixir CSV, Data Dragon 스냅샷은 저장소에 올리지 않는다. **정리한 결과와 「언제 어디서 어떻게 가져왔는지」만** 남긴다. 수집 스크립트가 있으면 원자료는 언제든 다시 만들 수 있다.

`.gitignore` 가 `data/` 와 `runs/` 를 막고 있고, pre-commit 의 `check-added-large-files` 가 2 MB 를 넘는 파일을 막는다.

### 출처를 섞지 않는다

같은 시계열에 여러 출처를 이어 붙이지 않는다. **집계 방식이 다르면 그 지점에서 값이 튀는데, 그것이 실제 변화처럼 보인다.**

이미 걸린 것 둘이 있다.

| 어긋남 | 내용 |
| --- | --- |
| 지역 | op.gg 는 한국(추정), u.gg 는 `world`. **섞으면 안 된다** |
| 티어 | 2023년 에메랄드 추가로 `platinum_plus` → `emerald_plus`. **모집단이 다르다** |

### 버전 표기를 믿지 않는다

같은 패치를 출처마다 다르게 부른다 — lol.ps 화면은 `16.15`, 내부 payload 는 `26.15`, Data Dragon 은 `16.16.1`. **연결 키는 이름이 아니라 날짜와 내부 ID 로 잡는다.**

### 아카이브 응답을 구분한다

웹 아카이브가 간헐적으로 일시 장애를 낸다. **응답이 11,832 B 로 오면 그것이다** — 「데이터가 없다」와 「아카이브가 죽었다」는 다르고, 실제로 한 번 오판할 뻔했다. 그리고 짧은 시간에 몰아 치면 **429** 가 난다.

### AI 를 어디에 쓰고 어디에 안 쓰는지 가른다

규칙으로 잡히는 것을 LLM 에 보내지 않고, 자연어 판단이 필요한 것을 규칙으로 우기지 않는다. **그 경계를 명시하는 것이 이 프로젝트의 축이다.**

| | 누가 |
| --- | --- |
| 수집 · 파싱 · 집계 · 통계 | 코드 |
| 자연어 판단, 규칙 추출, 사례 해석 | LLM |
| 조회 → 검색 → 판단 루프 | Agent |

### 통계 베이스라인 없이 LLM 성능을 주장하지 않는다

LLM 이 값을 하는지는 **단순한 방법과 비교해야** 안다. 로지스틱 회귀 같은 베이스라인을 먼저 만들고, 그것을 못 이기면 **못 이겼다고 적는다.** 그것도 결과다.

## 기술 결정 기록

다음에 해당하는 선택을 새로 하거나 바꿀 때는 **구현 전에** [`docs/adr/`](docs/adr/)에 ADR 을 쓴다.

| 대상 | 예 |
| --- | --- |
| **해당한다** | 언어·런타임·패키지 관리, 시뮬레이션 아키텍처, 데이터 스키마 형식, 검증 방법론, LLM 제공자와 호출 방식 |
| **해당하지 않는다** | 이미 정한 도구의 버전 갱신, 결함 수정, 문서 편집, 카드 한 장 추가 |

- 실제로 선택 가능한 후보를 최소 3개 비교하고, 선택 이유·다른 후보를 택하지 않은 이유·재검토 조건을 남긴다.
- 약한 후보를 숫자만 채우려고 넣지 않는다. 3개에 못 미치면 그 사실과 이유를 적는다.
- 기존 결정을 바꿀 때는 이전 ADR 을 지우지 않고 새 ADR 로 대체 관계를 기록한다.

번호와 파일명은 [`docs/adr/README.md`](docs/adr/README.md)를 따른다.

## Git 브랜치 규칙

- `main` 은 항상 돌아가는 상태를 유지한다.
- 모든 변경은 최신 `main` 에서 만든 짧은 수명의 작업 브랜치에서 진행한다.
- **저장소 초기 구성 커밋만 예외다** — 분기할 `main` 이 아직 없다. 그 뒤로는 `main` 에 직접 커밋하지 않는다.
- 브랜치 이름은 영문 소문자와 하이픈을 쓴다.

허용하는 접두사: `feat/`, `fix/`, `refactor/`, `docs/`, `test/`, `chore/`, `data/`, `exp/`

`data/` 는 카드·적 데이터 추가와 갱신, `exp/` 는 되돌릴 수 있는 밸런스 실험용이다. 예: `feat/damage-pipeline`, `data/act1-enemies`, `exp/rebalance-dead-cards`

저장소를 clone 한 직후 다음을 실행한다.

```bash
python3.11 -m venv .venv
source .venv/bin/activate
pip install -r requirements-dev.txt
pre-commit install
```

## 커밋 규칙

- Conventional Commits: `<type>(<optional-scope>): <summary>`
- 제목은 명령형으로 간결하게, 마침표 없이 쓴다.
- 하나의 커밋에 하나의 논리적 변경만 담는다.
- 관련 없는 파일을 `git add -A` 로 함께 stage 하지 않는다.

허용하는 타입: `feat`, `fix`, `refactor`, `docs`, `test`, `chore`, `data`, `exp`, `perf`, `revert`

예:

```text
feat(engine): add damage pipeline
test(engine): cover strength and vulnerable stacking
data: add Act 1 enemy intent patterns
docs: record damage calculation open questions
```

## 작업 원칙

- 저장소에 없는 게임 규칙이나 수치를 임의로 확정하지 않는다.
- 변경 범위를 작게 유지하고, 서로 다른 목적의 변경은 분리한다.
- API 키와 `.env` 를 커밋하지 않는다.
- 코드를 바꾸면 `ruff check`, `pytest`, `mypy` 를 돌린다. pre-commit 이 ruff 는 자동으로 잡지만 나머지 둘은 잡지 않는다.
- **실패와 되돌린 시도를 지우지 않는다.** 성공한 결과만 남은 기록은 왜 그 결과에 도달했는지 설명하지 못한다. 되돌린 접근과 그 이유를 커밋 본문이나 ADR 에 남긴다.

## 안전 규칙

- force push 와 커밋 이력 재작성을 하지 않는다. 예외는 비밀값을 이미 커밋된 이력에서 지우는 경우뿐이고, 그때는 ADR 에 무엇을·왜·언제 지웠는지 남긴다.
- `git reset --hard` 같은 복구가 어려운 명령은 명시적 승인 없이 실행하지 않는다.
