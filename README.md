# 리그 오브 레전드 밸런스 패치 예측

과거 패치 이력에서 **개발사의 밸런스 조정 기준을 역추출**하고, 그 기준으로 다음 패치 내용을 예측한 뒤, 실제 패치와 대조해 **적중률을 측정한다.** 그리고 **그 조정이 실제로 어떤 결과를 냈는지**까지 잰다.

```text
통계 상태  ──→  조정 행위  ──→  조정 후 결과
승률·픽률·밴율    누가·어느 방향     승률이 의도대로 움직였나
      └── 역추출 ──┘   └── 예측 ──┘   └── 효과 측정 ──┘
```

## 결과

52패치 · 8,767행 백테스트. **시간순 분할**(`15_13` 앞뒤)이고 무작위 분할이 아니다 — 그건 미래가 과거로 샌다.

| 과제 | 최고 성적 | 기준선 | 어떻게 |
| --- | ---: | ---: | --- |
| **① 누가 조정되나** | R-정확도 **25.8%** | 14.5% | 패치 안에서 줄 세우기 |
| **② 너프인가 버프인가** | AUC **0.880** · 정확도 **82.1%** | 48.7% | 조정된 챔피언만 |
| **③ 조정이 먹혔나** | 너프 **84.6%** · 버프 **75.1%** | — | 조정 안 된 챔피언을 대조군으로 |

**이 격차가 결론이다.**

> **누구를 건드릴지는 공개 지표로 잘 안 갈린다. 어느 쪽으로 건드릴지는 잘 갈린다.**

① 은 모델을 바꿔도 안 오른다 — 부스팅 +0.2%p, 이력 피처 −0.2%p, 프로 경기 피처 +0.1%p. **모델 한계가 아니라 데이터 한계다.**

어떤 규칙에도 안 걸리는 28건은 평균 승률이 **0.5000**, 너프 비율 **53.6%** 다. **밸런스 지점에 정확히 앉아 있어 공개 지표가 어느 쪽으로도 안 기운다.**

전체 비교표(26개 arm)와 검증 내역은 [`docs/results/README.md`](docs/results/README.md)에 있다.

## LLM·RAG 가 왜 졌는지 찾아서 고쳤다

처음 결과는 이랬다.

| arm | AUC |
| --- | ---: |
| 통계 회귀 + 프로 경기 | **0.880** |
| RAG 사례 검색 | 0.866 |
| LLM 규칙 | 0.807 |

**판단이 나쁜 게 아니라 해상도가 없었다.** 234행에 낸 점수가 규칙은 **5종**, RAG 는 **25종**, 통계는 **205종**이다. AUC 는 줄 세우기 점수라 5칸에 몰아넣으면 칸 안에 순서가 없다.

원인을 찾아 둘 다 고쳤다.

| | 무엇이 문제였나 | 어떻게 | AUC | 점수 종류 |
| --- | --- | --- | ---: | ---: |
| **LLM 규칙** | 한쪽 근거만 있으면 크기와 무관하게 `1.0`/`0.0` | 사전확률과 섞는다 | 0.807 → **0.836** | 5 → **19** |
| **RAG** | 거리에 프로 경기가 빠져 있었다 | 프로 피처를 거리에 넣는다 | 0.866 → **0.900** | 25 → **206** |

**고친 뒤 RAG(0.900)가 통계(0.880)를 넘는다.**

**해상도 진단은 규칙에만 맞았다.** RAG 도 같은 원인이라 보고 거리가중을 함께 넣었는데, 쪼개 보니 **거리가중의 기여는 0 이다** — 솔랭 피처만으로 켜면 0.866 → 0.860 으로 오히려 내려간다. 점수 종류가 25 → 206 으로 는 것은 사실이지만 **그것이 AUC 를 올린 게 아니다.** 올린 것은 프로 피처 하나다.

```
솔랭4 · k=25 · 다수결   0.866   ← 기준
솔랭4 · k=50 · 거리가중  0.860   거리가중만
프로+ · k=25 · 다수결   0.900   프로 피처만   ← 전부 여기서 온다
```

피처·이웃 수·가중 방식은 **학습 구간 3겹 시간순 교차검증으로 골랐다** — 평가 구간을 보고 고르면 그것이 곧 누출이다.

## 데이터

| 출처 | 역할 | 상태 |
| --- | --- | --- |
| **u.gg 아카이브** | 솔랭 승률·픽률·밴율 | ✅ **53패치** (`world` · `emerald_plus`) |
| **Data Dragon** | 정답지 — 버전 diff = 수치 변경 | ✅ 74버전 |
| **패치 노트** (공식 위키) | 조정 의도 — 버그 수정인가 밸런스인가 | ✅ 74패치 |
| **CommunityDragon** | 스킬 피해량·계수 (Data Dragon 에 없다) | ✅ **2,788쌍** · [ADR-0005](docs/adr/0005-skill-damage-data-source.md) |
| **Oracle's Elixir** | 프로 경기 픽·밴 | ✅ **29,977경기** · 72패치 |

**탐색했지만 안 쓴 것**: op.gg(지역이 섞인다) · lol.ps(범위가 좁다) · 국제 대회 일정([만들어도 못 쓴다고 확인](docs/spec/prediction-signals.md))

주소·형식·검증 상태는 [`docs/spec/data-sources.md`](docs/spec/data-sources.md)에 있다.

### 정제 — **버린 것이 핵심이다**

| 무엇을 | 왜 |
| --- | --- |
| **74 → 53패치** | 아카이브에 `emerald_plus` 가 있는 패치만. **다른 티어를 이어 붙이지 않는다** — 모집단이 다르면 그 지점에서 값이 튀고 그것이 실제 변화처럼 보인다 |
| **부분 커버리지 패치** | 챔피언이 일부만 있으면 픽률 분모가 안 선다 ([ADR-0002](docs/adr/0002-prediction-point-coverage-criterion.md)) |
| **전 챔피언 동시 변경** | 16.5 의 `attackdamageperlevel` 이 171종 전부 0 이 됐다. 스키마 변경이지 너프가 아니다 |
| **버그 수정** | 라벨이 담는 것은 **라이엇의 밸런스 의도**다. 되돌려 확인하니 AUC 가 0.618 → 0.643 으로 올랐다 |
| **중국 솔랭** | 텐센트가 따로 운영해 공개 통계에 구조적으로 없다. 집계 기준이 다른 값을 붙이지 않는다 |

## 검증

| | 무엇을 확인 | 결과 |
| --- | --- | --- |
| **누출** | 정답 라벨을 섞으면 성능이 무너지는가 | ✅ 전부 기준선으로 ([`check-leakage`](scripts/check-leakage)) |
| **시간순 분할** | 미래가 과거로 새는가 | ✅ 무작위 분할을 쓰지 않는다 |
| **재현** | 패널을 두 번 만들면 같은가 | ✅ md5 동일 |
| **라벨 품질** | 손 라벨이 기계 판정과 어긋나는가 | ✅ 채점 대상 407건 중 **충돌 0** |

## 정답지

| | |
| --- | --- |
| **방향 라벨** | **1,555종 · 52패치 · 커버리지 99.6%** |
| **밸런스 규칙** | 12개 제안 → **11개 통과** (향상 1.3배·적용률 2% 기준) |
| 만든 방법 | **대화 중 Claude 가 패치 노트를 읽고 붙였다.** 실행 시점에는 텍스트를 읽어 채점만 한다 |

기준과 되돌린 판단은 [`docs/spec/labeling-guide.md`](docs/spec/labeling-guide.md)에, 산출물은 [`ground_truth/`](ground_truth/)에 있다.

## 증명하려는 것

| # | 주장 | 상태 |
| --- | --- | --- |
| 1 | 공개 데이터만으로 조정 기준을 문서화할 수 있다 | ✅ 규칙 11개 통과 |
| 2 | 그 기준으로 다음 조정을 예측할 수 있다 | ✅ ① 25.8% · ② 82.1% |
| 3 | 조정 결과를 측정할 수 있다 | ✅ 너프 84.6% · 버프 75.1% |
| 4 | **어디까지 예측되고 어디부터 안 되는가** | ✅ **가장 정직한 결과** |
| 5 | LLM·RAG·Agent 가 값을 하는가 | ⚠️ LLM·RAG 는 쟀다. **Agent 는 아직 없다** |

### 증명하지 않는 것

- ❌ 우리가 개발사보다 밸런스를 잘 잡는다 — 판단을 **예측**하는 것이지 평가가 아니다
- ❌ 예측이 정확하다 — 정확도는 측정 대상이지 전제가 아니다
- ❌ 개발사의 내부 프로세스가 이렇다 — **관측된 패턴**일 뿐이다
- ❌ 라이엇의 조정이 옳았다·틀렸다 — 「너프했는데 승률이 올랐다」는 적어도, 「그러니 이렇게 했어야 한다」는 적지 않는다
- ❌ 이 도구로 밸런스를 대신 잡을 수 있다 — 판단 보조다

## 시작하기

```bash
python3.11 -m venv .venv
source .venv/bin/activate
pip install -r requirements-dev.txt
pre-commit install
```

원자료는 커밋하지 않는다. 수집부터 하려면:

```bash
./scripts/fetch-ddragon      # 버전별 챔피언 스펙
./scripts/fetch-patchnotes   # 공식 위키 패치 노트
./scripts/fetch-ugg          # 솔랭 통계 (웹 아카이브 경유)
./scripts/fetch-cdragon      # 스킬 피해량
```

패널을 만들고 리포트를 낸다:

```bash
./scripts/build-panel        # 8,767행 패널
./scripts/run-report         # 26개 arm 비교표 → runs/
./scripts/check-leakage      # 누출 검사
```

검사:

```bash
ruff check . && pytest && mypy
pytest --cov              # 커버리지 92%
```

`pre-commit` 이 커밋 전에 ruff 를 돌린다. **`pytest`(214개)와 `mypy` 는 자동으로 돌지 않으므로 직접 실행한다.**

## 구조

```text
src/lol_balance/     수집 · 파싱 · 패널 · 예측 · 평가
scripts/             실행 진입점 (수집 · 라벨링 · 리포트 · 검증)
ground_truth/        정답지 — 방향 라벨 1,555종 (커밋한다)
rules/               밸런스 규칙 12개 (커밋한다)
docs/spec/           데이터를 어디서 어떻게 받는가
docs/adr/            기술 결정 기록 5건
docs/results/        측정 결과
data/ · runs/        원자료와 실행 산출물 (커밋하지 않는다)
tests/               214개
```

작업 규칙은 [`AGENTS.md`](AGENTS.md)(= [`CLAUDE.md`](CLAUDE.md))에 있다.

## 기술 스택

| 기술 | 용도 |
| --- | --- |
| Python 3.11 | 전반 |
| numpy · scikit-learn | **통계 베이스라인** — LLM 이 값을 하는지 비교 대상 |
| pydantic | 구조화 출력 강제 |
| beautifulsoup4 | 패치 노트 HTML 파싱 |
| sqlite (표준 라이브러리) | 패널 저장 |
| pytest · ruff · mypy · pre-commit | 코드 품질 — **테스트 214개 · 커버리지 92%** |

**의존성은 직접 import 하는 것만 선언한다.** 전이 의존성은 적지 않는다 —
`scipy` 는 `scikit-learn` 이 끌어오지만 우리가 직접 쓰지 않으므로 안 적는다.

**실행 시점에 LLM API 를 부르지 않는다.** 모델이 만든 산출물(라벨·규칙)은 저장소에 텍스트로 있고, 코드는 그것을 읽어 채점한다. 근거는 [`ground_truth/README.md`](ground_truth/README.md).

### 도입하지 않는 것

| 기술 | 사유 |
| --- | --- |
| 파인튜닝 (LoRA·SFT) | 데이터가 표에 가까워 언어 모델의 강점이 안 살아난다. **판단 근거가 안 남아** 기획 산출물이 되지 못한다 |
| 벡터 검색을 수치 조회에 | SQL 이 더 정확하고 빠르다. 벡터는 **텍스트**에만 쓴다 |
| LangChain / LangGraph | 도구 몇 개짜리 단일 루프라 오케스트레이션이 과하다 |
| FastAPI | 지금은 API 로 감쌀 이유가 없다 |

## 남은 것

**에이전트(A6) 한 칸이다.** 검색기 셋([`retrieval.py`](src/lol_balance/retrieval.py))은 완성돼 있고 `as_of` 로 미래도 막혀 있다. 없는 것은 그것을 쥐고 도는 판단 루프뿐이다.

미뤄 둔 것은 [`docs/followups.md`](docs/followups.md)에 있다.
