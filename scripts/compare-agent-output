#!/usr/bin/env python3
""".env에 OPENAI_API_KEY 등록 후 실행. 준비만 할 때 --prepare, 재채점 --score."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "src"))

from lol_balance.agent.output_comparison import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
