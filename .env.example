# 복사해서 .env 로 쓴다. .env 는 커밋하지 않는다.
#   cp .env.example .env

# 패치 노트 구조화, 규칙 추출, 예측 에이전트에 쓴다.
# 데이터 수집과 통계 베이스라인은 이 값이 없어도 돌아간다.
ANTHROPIC_API_KEY=

# 규칙 추출과 예측에 쓸 모델.
PATCHLENS_LLM_MODEL=claude-opus-5

# 학습·평가 분할과 표본 추출의 기본 시드. 같은 시드 → 같은 분할.
PATCHLENS_SEED=20260824
