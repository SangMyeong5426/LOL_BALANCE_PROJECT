# 복사해서 .env 로 쓴다. .env 는 커밋하지 않는다.
#   cp .env.example .env

# Phase 2 (페르소나 정책 생성, 분석 리포트)에서만 필요하다.
# Phase 0-A / 0-B / 1 은 LLM 없이 돌아간다.
ANTHROPIC_API_KEY=

# 정책 생성·분석에 쓸 모델. 페르소나당 수 회만 호출하므로 최상위 모델을 쓴다.
ASCENT_LLM_MODEL=claude-opus-5

# 시뮬레이션 기본 시드. 같은 시드 → 같은 결과.
ASCENT_SEED=20260824
